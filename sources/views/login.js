import { JetView } from "webix-jet";

export default class Login extends JetView {
    config() {
        return {
            template: "Y aquí va el login"
        }
    }
    init() {
        
    }
}